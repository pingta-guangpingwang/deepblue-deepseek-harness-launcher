# 安全政策

## 支持范围

只对最新稳定版和当前 `main` 分支提供安全修复。历史版本发现问题后应优先升级。

## 报告漏洞

请使用 GitHub 仓库的 **Security → Report a vulnerability** 私密报告入口；不要在公开 Issue 中粘贴 API Key、密码、访问令牌、私人日志或可直接利用的漏洞细节。

报告应尽量包含受影响版本、复现步骤、预期影响和最小必要日志。维护者会先确认收到，再评估修复与披露时间。

## 发布边界

- 正式更新目录必须通过仓库内置 Ed25519 公钥验证；模块还必须匹配固定字节数与 SHA-256。
- Windows Authenticode 和更新目录 Ed25519 是两套不同信任链，任何一套都不能冒充另一套。
- 不要求用户关闭 Defender、SmartScreen、Smart App Control 或添加安全软件排除项。
