# Product

<!-- impeccable:product-schema 1 -->

## Platform

adaptive

## Users

Windows 10/11 64 位的新手用户，希望使用 DeepSeek Harness，但不熟悉 Git、Node.js、pnpm 或命令行环境配置。

## Product Purpose

深蓝 DeepSeekHarness 启动器把环境检测、依赖安装、国内外下载源选择、版本管理、AI 工具、模型路由、免费皮肤与网页宠物、工作区选择和 Harness 进程启停集中到一个桌面界面。新闻、AI 游戏、AI 工具、Skill、工作流与职场进化直接读取 AI历史书网站正在使用的最新公开目录；只有长篇课程按需打开教学网站。工具与 Skill 收藏必须登录，并写入同一 AI历史书账号的统一收藏，不在启动器本地另存一份。成功意味着用户下载标准安装 EXE 后双击即可完成安装并进入启动器，再从首页启动 Harness。

## Positioning

启动器同时提供在线轻量版与完整离线版：在线版自动选择可用镜像安装 Harness，离线版携带运行依赖并可在无网络时启动。

## Operating Context

用户下载 Windows 标准 NSIS 安装 EXE，双击后由安装程序显示进度并允许选择程序目录，创建桌面与开始菜单快捷方式后自动启动 Electron 桌面应用。首次启动先确认运行资源目录，再开始拉取 Node、Harness 和按需模块；之后可在设置中安全迁移资源目录或修复快捷方式。发布程序不调用隐藏 PowerShell，也不绕过执行策略。用户在首页查看环境与源站状态、选择工作区并启动本地 Harness Web 服务；启动后浏览器打开 Harness 界面。模型调用需要用户自行配置 API 密钥，首页提供 DeepSeek 官网充值入口。

## Capabilities and Constraints

- 当前发布平台为 Windows 10/11 64 位。
- Windows 整合包必须使用标准 NSIS 引导安装 EXE，并允许选择程序目录，不得通过 BAT 或 PowerShell 自释放二进制、隐藏执行或绕过执行策略。
- 程序目录与运行资源目录必须分离。切换运行资源位置只复制启动器管理的数据，成功切换后保留旧目录，不得自动删除用户资料。
- 在线轻量版首次准备 Harness 时需要联网；完整离线版不依赖联网启动。
- 启动器不得把 API 密钥写入发布包、项目目录、普通配置文件或日志。用户主动保存的模型 Key 只用 Electron `safeStorage` 加密留在本机，启动 Harness 子进程时才解密为环境变量。
- DeepSeek V4 Flash / Pro 是默认模型连接；其他平台或自定义接口只有用户点击添加并保存后才进入全局模型切换列表。
- 模型模板默认优先展示国内平台，覆盖 DeepSeek、通义千问、豆包、Kimi、智谱、百度千帆、腾讯 TokenHub、MiniMax、阶跃星辰、硅基流动和百川；已知平台只允许从标注更新时间的官方模型目录中勾选，名称、协议、API 地址和模型 ID 不由用户手填，模板不代表账号已经开通模型权限。
- 模型用量只汇总 Harness 本地会话中服务商返回的 Token 字段；启动器不猜价格、余额或赠送额度，金额跳转服务商官方账单查看。
- 模型目录提供一次一图的多模态实测：图片不落盘，一次点击只调用一次所选服务商接口，显示文字结果、耗时和服务商返回的 Token；模型不支持图片时必须明确提示，不自动改用其他模型。
- 新闻最新 10 条和热门排行无需登录；新闻行双击或按 Enter 后必须在启动器弹窗阅读摘要、影响对象和行动建议，登录后在同一页面继续展开更多新闻。游戏、能力资源与职业目录无需登录并在启动器内展示。
- 提示词、Skill、工作流、知识库、AI 工具和智能体使用独立侧栏入口；同类模型与 Agent 排在可复用能力资源之后。能力卡片单击或键盘 Enter/空格直接打开原生详情层，呈现输入、步骤、产出、开源地址、Stars、热度、评分与用户评价，并支持复制和账号收藏。
- 资源加入安装列表后由用户再次确认：Skill 只写入 Harness 原生 `DSH_HOME/skills` 目录并默认禁止模型自动调用，提示词、工作流和知识库写入本机受控能力库。启动器不得从任意公开仓库下载并执行代码。
- 评价公开可读，发表评论必须登录 AI历史书账号；账号入口固定在左下角，登录会话由 Chromium/Windows 加密记住。
- 账号登录使用启动器内的受信窗口，登录后的 HttpOnly 会话由 Chromium/Windows 加密保存，启动器不保存网站明文密码。
- 两个公开下载链接与签名清单链接保持稳定，视觉升级通过覆盖相同 OSS 对象发布。
- API 充值入口只允许跳转到 `https://platform.deepseek.com/usage`，不得代收密钥或支付信息。
- 原始 DeepSeek Harness 项目采用 MIT License；启动器是非官方启动辅助工具。
- 皮肤商店只接收许可允许再分发、来源可验证的媒体，不执行皮肤仓库提供的 CSS 或 JavaScript；目录和媒体必须通过签名、大小和 SHA-256 校验。
- 宠物商店采用同样的签名和媒体校验，不加载第三方脚本；在线宠物按需下载，本机导入文件不上传。宠物位置只保存在 Harness 浏览器的本地存储中。

## Brand Commitments

- 产品名称为“深蓝 DeepSeekHarness 启动器”。
- 使用 DeepSeek Harness 源码自带的鲸鱼标志作为启动器图标和首页主要品牌元素。
- 界面遵循 DeepSeek 的白色、黑灰文字、浅灰圆角区域和 DeepSeek 蓝色强调风格。
- 操作文案直接、克制，面向没有开发环境经验的新手。

## Evidence on Hand

- 启动器真实仪表盘：`docs/qa/dashboard-1440.png`。
- DeepSeek Harness 鲸鱼标志来源：`E:/ObsidianRes/obRes1/deepseekHarness/apps/web/public/favicon.svg`。
- DeepSeek Harness 中文设置界面：`E:/ObsidianRes/obRes1/deepseekHarness/docs/user/guide/providers-models-page.zh.png`。
- 在线安装、断网启动、插件生命周期、版本回滚和公网下载哈希均有本地测试记录；不得虚构用户评价、销量或官方背书。

## Product Principles

1. 新手无需理解源码和工具链即可启动。
2. 运行状态、下载源和修复动作必须清晰可见。
3. 安装程序与启动器的安装、更新、修复和启动任务必须显示产品名称、当前步骤和进度，不能让新手面对没有解释的命令行等待。
4. 在线失败时提供可解释的源切换，断网时提供完整离线方案。
5. 品牌识别服务于操作，不遮挡任务和状态。
6. 所有发布更新保持可验证、可回滚且不包含密钥。
7. 已安装模块有更新时，启动器必须先明确列出模块、版本与下载量；用户确认后才下载，显示模块列表和总进度，全部安装成功后自动重启。模型提供方目录属于已签名的小型数据目录，可独立更新，不要求替换启动器壳，也不能覆盖用户已经保存的连接与 Key。
7. 启动器内直接展示高频公开目录与账号入口；长课程、创作后台和需要宽阅读面的编辑功能继续由网站承载。
