# Native and remote workspace hierarchy

- Scope: AgentWorkspacePage, LocalAgentWorkspace, shared hierarchical navigation, approval surface and related CSS; Operate mode. Preserve group-chat entry and all other launcher pages.
- User-approved structure: device → agent → project → conversation; eight connectable adapter entries, truthful per-capability state, one-time agent scope plus separate per-operation native approvals.
- Delegated choice: user explicitly asked to check logic and proceed. Three high-fidelity comps were generated and inspected; choose `.impeccable/mocks/workspace-hierarchy-a.png` because the project tree remains visible while a separate conversation list stays scannable. B nests too much in one tree; C separates projects from their visible parent.
- Existing visual authority: DESIGN.md, flat white/light-gray, blue only for active selections/actions, Chinese system sans, Lucide line icons. No new world or seed; this is a precisely specified restructuring within the incumbent app.
- First viewport: existing global launcher sidebar; workspace source tabs; 270px device/agent/project explorer; 220px conversation list; dominant remaining chat. Narrow screens navigate one pane at a time; explicit Back and breadcrumb preserve location.
- Proof: actual device binding, adapter capability states, native project/session records; inline approval with exact scope, reason and approve-once/refuse, with pending/sending/confirmed/unknown distinctions. No numeric zero for unavailable readers; no all-green state copied from the mock.
- Do not literalize mock demo data, timestamps, counts, generated icons, chat claims or CLI command. Use real/synthetic-labeled fixtures only. Approval grants no broader permission than the current operation; result is confirmed only after native state evidence.
- Inventory: global shell/header/tabs: existing React/CSS; tree rows/disclosure/status: semantic buttons, nested lists, Lucide; conversation rows: React buttons, text wrapping, search; chat: existing ChatMessage markdown/composer; approval: semantic section, command/path pre blocks and explicit decision buttons; all use existing tokens. No raster assets ship.
- Constraints: retain source identity, selection isolation, drafts and settings. Unowned old connectors are a separate group; remote controls cannot target local Host by accident. Native compatibility checked before enabling approval actions. Offline/cancelled/expired requests never auto-approve.
- Acceptance: 8 entries, no horizontal adapter strip, device/project/thread isolation, actual native pending request → decision forwarding → status confirmation; desktop/narrow screenshot QA and existing regression suites.
