# 登录与设备自动连接验收（2026-09-08）

## 已完成

- 生产审计确认：21:18 密码重置成功，随后的邮箱登录被前端错误转换为账户别名。兼容层已改为登录时保留邮箱原值；普通中文/数字账号的既有映射不变，密码不作改写。
- 生产登录脚本已备份并替换，SHA-256 为 `643163ffe3d77ac8b096b9bbec0fcb6efb5044c8bb3767080a995eb3db776616`；设备改名接口也已备份、PHP 语法校验、部署和回读核对。没有数据库迁移。
- 启动器登录后检查自己的设备列表；首次自动登记，同一安装保留 installationId。云端移除后仅在下次登录/启动检查时恢复，移除操作不会被当前心跳立即撤销。
- 设备名可编辑，ID、密钥和项目不因改名变化；手动暂停保留。恢复设备不会自动启动智能体或扩大原授权目录。
- 本机实际安装 `agent-host 1.0.0+ec7354b214dc` 与 `launcher-ui ui-ff8c43168f93b501`，基础内核仍为 0.10.34。两次真实重启证实记住登录、原设备 ID 不变、改名和暂停状态持久化。
- 实际界面改名为“我的工作电脑（验收）”成功后恢复原名。空名称保存按钮禁用。截图：`output/playwright/device-renamed-20260908.png`、`output/playwright/device-auto-connect-restored-20260908.png`。
- 五个原托管连接已由界面恢复，均为 online / ready / busy=false；TRAE 仍明确 stopped / unavailable，不冒充可用。
- Launcher 服务测试 22 项通过，连同旧配置与连接指引共 33 项；TypeScript 通过。网站鉴权/设备专项 7 项通过；核心静态与契约门禁 53 组通过。新增 MySQL 用例尚未在本次实际 MySQL 环境运行。

## 明确未完成

- 服务器本地保存的管理员旧密码实测返回“密码错误”。没有重置用户密码，也没有提取启动器令牌冒充网页登录。已请求用户在右侧浏览器以刚重置的密码正常登录。
- 密码修复后的本人真实登录、真实网页/手机远程唤起和发送验收仍等浏览器登录，不计为通过。
- 新模块仅完成本机安装验收，尚未签名发布到公共更新目录；旧公共目录仍可能提示替换本地预览模块，请勿用旧目录覆盖预览版。
- 会话群仍为架构设计阶段，本次没有宣称已实现。

## 2026-09-09 状态修订

- “等待网页登录才能开始验收”已被 9 月 8 日晚间结果部分覆盖：使用启动器保留的真实统一账号会话进入线上工作台后，Claude 完成网页原会话收发，五种支持的适配器完成网页恢复托管，QClaw 完成 390×844 手机收发与刷新持久化。仍未完成“从全新登录页使用重置后密码登录”和其余四适配器完整手机发送矩阵。
- 本地实现继续保持“登录后自动登记设备、installationId 不变、设备可改名、手动暂停不被自动恢复、移除后仅在新登录检查时重建”；新增回归覆盖“云端移除且本机处于手动暂停”的组合边界。
- 连续未登录轮询现在是幂等的，不再每 15 秒重写 safeStorage 密文和备份。
- 新本地候选为 `agent-host 1.0.0+5060df99303d` / `launcher-ui ui-9c62edfb1920812c`，尚未安装到正在托管五个智能体的实际启动器，也未签名发布。公共版本仍为 `f979af8c34c9` / `be272626e5dc477d`。
